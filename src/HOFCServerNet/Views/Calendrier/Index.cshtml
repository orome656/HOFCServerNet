@using HOFCServerNet.Data.Models;
@model HOFCServerNet.ViewModels.Calendrier.CalendrierViewModel

@{
   List<Match> matchs = Model.Calendriers;
}
@foreach (Match match in matchs)
{
    @await Html.PartialAsync("~/Views/Match/Match.cshtml", match);
}
